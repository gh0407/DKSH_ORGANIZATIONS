#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, sum;
    sum = 0;
    for(i = 0; i <= 100;)
    {
        sum = sum + i;
        i = i + 2;
    }
printf("0부터 100까지의 짝수 합은 %d이다", sum);
return 0;
}
